spring.application.name=domain
spring.datasource.url=jdbc:postgresql://localhost:5432/fluxverde
spring.datasource.username=fluxverde
spring.datasource.password=${DB_PASSWORD}

# Use UTC for internal date/time handling end-to-end.
spring.jackson.time-zone=UTC
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
